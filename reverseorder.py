# 1) Ask the user to enter a base number and store it in `base`.
base = int(input("Enter a base number: "))
# 2) Ask the user to enter an exponent and store it in `exp`.

# 3) Create a variable called `result` and set it to 1.
#    (This will hold the growing power value.)

# 4) Loop from 1 up to `exp`:
#    - Multiply `result` by `base`
#    - Store the new value back into `result`
#    - This builds the power step by step.

# 5) Print the base number (`base`).

# 6) Print the exponent (`exp`).

# 7) Print the final calculated value (`result`).
